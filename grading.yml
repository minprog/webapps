grades:
    homepage:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && ((points.to_i.to_f/5*9+1).round(1) || -1) || 0
        hide_calculated: true
    books:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && ((points.to_i.to_f/5*9+1).round(1) || -1) || 0
        hide_calculated: true
    flack:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && ((points.to_i.to_f/5*9+1).round(1) || -1) || 0
        hide_calculated: true
    pizza:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && ((points.to_i.to_f/5*9+1).round(1) || -1) || 0
        hide_calculated: true
    final_project:
        type: pass
        subgrades:
            code: integer
            repo: integer
            design: integer
            docs: integer
        calculation: ((code.to_i+repo.to_i+design.to_i+docs.to_i).to_f/20*9+1).round(1)
        hide_calculated: true

calculation:
    apps:
        projects: 1

projects:
    show_progress: true
    submits:
        homepage: 1
        books: 1
        flack: bonus
        pizza: 1

final:
    show_progress: true
    submits:
        final_project: 0
