grades:
    homepage:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && (points.to_i.to_f/5*9+1 || -1) || 0
        hide_calculated: true
    books:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && (points.to_i.to_f/5*9+1 || -1) || 0
        hide_calculated: true
    flack:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && (points.to_i.to_f/5*9+1 || -1) || 0
        hide_calculated: true
    pizza:
        type: float
        subgrades:
            repo_ok: boolean
            done: boolean
            points: integer
        calculation: (done == -1 && repo_ok == -1) && (points.to_i.to_f/5*9+1 || -1) || 0
        hide_calculated: true
    final_project:
        type: pass
        subgrades:
            link_ok: boolean
            design_aanwezig: boolean
        calculation: (link_ok == -1 && design_aanwezig == -1) && -1 || 0
        hide_calculated: true

calculation:
    eind-apps:
        projects: 1

projects:
    show_progress: true
    submits:
        homepage: 0
        books: 1
        flack: 1
        pizza: 1

final:
    show_progress: true
    submits:
        final_project: 0
