grades:
    homepage:
        type: pass
        subgrades:
            repo_ok: boolean
            done: boolean
        calculation: (done == -1 && repo_ok == -1) && -1 || 0
        hide_calculated: true
    books:
        type: pass
        subgrades:
            repo_ok: boolean
            done: boolean
        calculation: (done == -1 && repo_ok == -1) && -1 || 0
        hide_calculated: true
    flack:
        type: pass
        subgrades:
            repo_ok: boolean
            done: boolean
        calculation: (done == -1 && repo_ok == -1) && -1 || 0
        hide_calculated: true
    pizza:
        type: pass
        subgrades:
            repo_ok: boolean
            done: boolean
        calculation: (done == -1 && repo_ok == -1) && -1 || 0
        hide_calculated: true

calculation:
    eind-apps:
        projects: 1

projects:
    show_progress: true
    submits:
        homepage: 0
        books: 1
        flack: 1
        pizza: 1
